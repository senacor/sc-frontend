pipeline {

    agent any

    environment {
        DACO = credentials("secVal")
    }

    stages {
        stage('building jar') {
                agent { docker 'node' }
                steps {
                    sh "npm build"
                }
        }
    }
}
